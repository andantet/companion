archivesBaseName = 'mccic-config'
version = getSubprojectVersion(project)

moduleDependencies(project, [
        'mccic-api'
])

loom {
    accessWidenerPath = file("src/main/resources/${archivesBaseName}.accesswidener")
}
